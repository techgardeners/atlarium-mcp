# Windsurf Example

Copy `mcp_config.json` into `~/.codeium/mcp_config.json` or add the `atlarium`
entry through Windsurf settings.

Then restart Windsurf or refresh Cascade MCP servers.

Prompt:

```text
Use Atlarium to find water parameter guidance for nitrate.
```

No authentication headers are required.
