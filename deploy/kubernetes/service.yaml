apiVersion: v1
kind: Service
metadata:
  name: atlarium-mcp
  labels:
    app.kubernetes.io/name: atlarium-mcp
    app.kubernetes.io/part-of: atlarium
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: atlarium-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
